import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  likeComtainer: {
    flexDirection: "row",
  },
  footerText: {
    color: "#0ba376",
    marginLeft: 5,
    fontWeight: "500",
  },
});

export default styles;
